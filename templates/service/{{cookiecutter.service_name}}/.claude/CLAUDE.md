# {{cookiecutter.service_name}}

Generated by `paved-road`'s scaffold. Console-first DX (PRD §11) lands in
session 4 via `.claude/skills/ship` (push → gates → promote) and
`.claude/skills/watch` (tail the pipeline, surface agent narration). Both are
placeholders until then — don't build ahead of that session.

No `.mcp.json` here — GitHub MCP is a dev-environment tool for the
`hops.ai-demo` planning workspace only (its CLAUDE.md §"Installed tooling"),
not something this generated repo ships with.
