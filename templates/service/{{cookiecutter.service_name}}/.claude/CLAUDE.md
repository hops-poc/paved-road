# {{cookiecutter.service_name}}

Generated by `paved-road`'s scaffold. This repo's thinness is the product demo, not a
placeholder — the pipeline, policies, and IAM live in `paved-road`, not here. Keep it thin.

`.github/workflows/ci.yml` calls `paved-road`'s reusable workflows at a pinned SHA. Don't
move pipeline logic into this repo; change it in `paved-road` and bump the pin via a PR.

`.claude/skills/ship` and `.claude/skills/watch` are **not built** — console-first DX
(PRD §11) is the remaining gap. The pipeline they would wrap already works; drive it with
`gh` in the meantime. Each SKILL.md states what it needs to do.

No `.mcp.json` here — GitHub MCP is a dev-environment tool for the `hops.ai-demo` planning
workspace only, not something this generated repo ships with.
