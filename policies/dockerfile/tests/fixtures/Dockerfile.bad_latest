# Dockerfile.good with exactly one thing broken: unpinned FROM (scenario 2).
FROM oven/bun:latest AS base
WORKDIR /app

COPY package.json bun.lock* ./
RUN bun install --frozen-lockfile --production

COPY src ./src

USER bun
EXPOSE 3000
ENTRYPOINT ["bun", "run", "src/server.ts"]
